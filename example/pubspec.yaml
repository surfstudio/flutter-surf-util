name: example
description: Example
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


dev_dependencies:
  flutter_test:
    sdk: flutter
  surf_lint_rules: ^1.0.0
  surf_util:
    path: ../

flutter:
  uses-material-design: true
